import styled from 'styled-components';

export const Link = styled.a`
  color: var(--secondary);
  cursor: pointer;
  font-weight: 400;
  font-size: 14px;
  line-height: 18px;
  text-decoration-line: underline;
`;
